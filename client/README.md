# Virtual Cosmos Client

React, Vite, PixiJS and Tailwind frontend for the proximity chat demo.

```bash
npm install
npm run dev
```

The app expects the Socket.IO server at `http://localhost:5000`. To change it, copy `.env.example` to `.env` and update `VITE_SERVER_URL`.
